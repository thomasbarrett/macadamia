{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": ["--privileged"],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools"
            ]
        }
    },
    "postStartCommand": "git config --global --add safe.directory ${containerWorkspaceFolder}"
}
